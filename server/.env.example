# ===========================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!

# ===========================================
# APPLICATION SETTINGS
# ===========================================
NODE_ENV=development
PORT=5000

# ===========================================
# DATABASE
# ===========================================
# Local MongoDB (Development):
# MONGO_URI=mongodb://localhost:27017/expense-tracker

# MongoDB Atlas (Production):
# Get from: https://cloud.mongodb.com/
# Format: mongodb+srv://username:password@cluster.mongodb.net/expense-tracker?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string_here

# ===========================================
# SECURITY
# ===========================================
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# MUST be at least 64 characters for production!
JWT_SECRET=your_super_secret_jwt_key_at_least_64_characters_long

# ===========================================
# EMAIL CONFIGURATION
# ===========================================
# Choose ONE of the following email providers:

# OPTION 1: Brevo (formerly Sendinblue) - FREE TIER: 300 emails/day
# Sign up: https://www.brevo.com/
# Get SMTP credentials from: Account > SMTP & API
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_EMAIL=your_brevo_login_email
SMTP_PASSWORD=your_brevo_smtp_key
FROM_EMAIL=your_verified_sender_email@example.com
FROM_NAME=Expense Tracker

# OPTION 2: Gmail (For testing only, not recommended for production)
# Enable 2FA and create App Password: https://myaccount.google.com/apppasswords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_EMAIL=your_email@gmail.com
# SMTP_PASSWORD=your_16_character_app_password
# FROM_EMAIL=your_email@gmail.com
# FROM_NAME=Expense Tracker

# OPTION 3: SendGrid - FREE TIER: 100 emails/day
# Sign up: https://signup.sendgrid.com/
# Get API key from: Settings > API Keys
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_EMAIL=apikey
# SMTP_PASSWORD=your_sendgrid_api_key
# FROM_EMAIL=your_verified_sender@example.com
# FROM_NAME=Expense Tracker
